# معمارية Netora وقرارات الأمان

## نطاق النسخة الحالية

تعمل **Netora** حاليًا كتطبيق React/TypeScript وExpress/tRPC متعدد المستأجرين. يعتمد الوصول على جلسة مصادق عليها ثم عضوية مؤسسة فعّالة يستخرجها الخادم؛ لا يُعتمد معرّف مؤسسة من متصفح المستخدم وحده. لا يمثل هذا المستند تشغيلًا فعليًا لـMikroTik أو FreeRADIUS؛ فالإعدادات الحالية تحفظ مرجع سر فقط وتضع الأعمال المحددة في طابور.

| طبقة | المسؤولية المنفذة | حاجز الأمان |
|---|---|---|
| الهوية | جلسة OAuth المدمجة | الإجراءات المحمية ترفض الجلسة الغائبة |
| سياق المؤسسة | عضوية خادمية وفق slug | كل قراءة وكتابة تشغيلية مقيدة بـ`organizationId` المستخرج |
| التفويض | أدوار owner/manager/operator/accountant/support/viewer | صلاحية مفصلة لكل إجراء؛ لا يكفي إخفاء زر في الواجهة |
| البيانات | Drizzle/MySQL | تحقق إدخال Zod واستعلامات مقيدة بالمؤسسة |
| التدقيق | سجل أحداث منقح | لا تُسجل كلمات مرور أو رموز أو مراجع أسرار |
| الملفات | تخزين كائنات منفصل | الميتاداتا فقط في القاعدة، ورابط وصول مؤقت بعد فحص العضوية |
| الفوترة | فواتير ودفعات وقيود مزدوجة | يتحقق الخادم من حالة الفاتورة والرصيد ويعكس قيد دفعة مؤكدة فقط؛ لا يُنفّذ رد مال لدى بنك أو بوابة |

## نموذج عزل المستأجرين

يضيف إجراء المستأجر هوية المؤسسة إلى السياق على الخادم بعد إثبات العضوية. تمر عمليات الراوترات والعملاء والباقات والبطاقات والجلسات والفواتير والدعم والملفات والتكاملات عبر هذا السياق. تدقق عمليات الإنشاء والتعديل الأساسية بالفاعل والمورد والنتيجة ومعرف الطلب، مع تنقيح الحقول ذات الحساسية الأمنية.

> لا تُعد الواجهة مصدرًا للتفويض. إن تغيير قيمة في الطلب لا يمنح وصولًا إلى بيانات مؤسسة أخرى؛ فالخادم يعيد اشتقاق نطاق المؤسسة قبل تنفيذ الاستعلام.

تستقبل قوائم الجلسات والفواتير ودفعات البطاقات وتذاكر الدعم حدود ترقيم وعبارة بحث ومرشح حالة متحققًا منها. يبدأ كل استعلام بشرط المؤسسة المستخرج من السياق، ثم يضيف المرشح الاختياري؛ ولا يعيد سجل الدفعات مفاتيح أسرار أو تجزئات رموز البطاقات أو مفاتيح تخزين الملفات. تُدرج قوالب الدعم نصها يدويًا في محرر الرد قبل الإرسال، ولا تنشئ رسالة أو تنبيهًا خارجيًا تلقائيًا. تقيّد تغييرات حالة التذكرة بانتقالات مسموحة داخل معاملة، وتمنع إعادة فتح التذكرة المغلقة في هذه النسخة.

سجل التدقيق واجهة قراءة فقط: يمكن للمستخدم ذي الصلاحية البحث في نوع العملية أو المورد وتصفية النتيجة وترقيم العرض داخل مؤسسته، بينما يظل تنقيح البيانات الوصفية وخيار الحقول المعروضة خادميين. لا يتيح البحث استرداد كلمات مرور أو مراجع أسرار أو مفاتيح تخزين أو محتوى ملفات، ولا يتيح أي تعديل للسجل.

تستخدم قائمة الملفات بحثًا بالاسم أو النوع وتصنيفًا وترقيمًا بعد تثبيت شرط المؤسسة على الخادم. لا تعيد استجابة القائمة مفتاح التخزين الكائني؛ وعند طلب فتح ملف تتحقق عملية مستقلة من عضوية المؤسسة وملكية الملف قبل إصدار رابط وصول موقّع قصير العمر.

## حدود المالية المنفذة

يسجل النظام إنشاء المسودة وإصدار الفاتورة والقبض المؤكد في قيود مزدوجة متوازنة. يمكن للمفوض تسجيل **عكس محاسبي** لدفعة مؤكدة؛ فيحوّل حالتها إلى `refunded` وينشئ قيدًا معاكسًا ويعيد الفاتورة إلى `issued` ما لم تبقَ مدفوعة بالكامل بدفعات مؤكدة أخرى. لا يشكل هذا الإجراء تعليمات إلى بنك أو مزود دفع، ولا يثبت إرجاع المال للعميل.

> ما زالت المصروفات والتسويات الضريبية والاسترداد الخارجي والتقارير الدورية متوقفة على سياسة محاسبية معتمدة ودمج مزود الدفع الملائم.

يمكن لسجل الدفعات أن يبحث في المرجع أو رقم الفاتورة وأن يصفّي الحالة ويرقّم النتائج داخل المؤسسة فقط. لا يوسّع ذلك صلاحية العكس المحاسبي أو ينشئ اتصالًا ببنك أو بوابة دفع، وتظل عمليات العكس مدققة ومقيدة بالدفعة المملوكة للمؤسسة.

## حدود الخطة والحجز الذري

تحتفظ المؤسسة بعدّاد مستقل للراوترات والعملاء. عند وجود اشتراك نشط أو تجريبي، يحجز إنشاء راوتر أو عميل خانة واحدة عبر تحديث مشروط داخل معاملة قاعدة البيانات؛ لا يستمر الإدخال إلا إذا كان العداد أقل من حد الخطة في لحظة التحديث. يضمن تضمين الإدخال والحجز وفحص الموقع في معاملة واحدة التراجع عن الحجز إن فشل الإدخال. تمت إعادة ملء العدادات من الموارد الموجودة بعد إضافة الحقول، وتحقق استعلام المطابقة من عدم وجود فروق في قاعدة البيانات وقت الترحيل.

> لا يغني هذا الحارس عن مراقبة دورية للاتساق في بيئة الإنتاج، ولا يغطي موارد مستقبلية غير الراوترات والعملاء إلا بعد تصميم عدادات وحجز مخصصين لها.

## تكامل الشبكة المستهدف

تستخدم البنية المستهدفة RADIUS للمصادقة والمحاسبة، وRouterOS API-SSL أو REST عبر HTTPS للإدارة. يجب أن تعمل الاتصالات في عامل منفصل قابل للمراقبة، لا في طلب tRPC متزامن ولا في المتصفح. توصي وثائق MikroTik باستخدام API-SSL أو REST HTTPS بدل HTTP غير المشفر، وتوضح منافذ RADIUS الافتراضية للمصادقة والمحاسبة [1] [2].

| عملية | عقد النسخة الحالية | ما يلزم قبل التشغيل الحقيقي |
|---|---|---|
| تسجيل راوتر | بيانات وصفية + مرجع سر فقط | شبكة خاصة/VPN، vault، فحص هوية وقراءة لاحقة |
| فحص راوتر | job محدود يسمح بـhealth/identity فقط | عامل دائم وtimeouts وread-back وقياس نتائج |
| إسقاط RADIUS | job projection محدود بلا credentials | Policy compiler وبيئة FreeRADIUS مخبرية واختبارات attributes |
| CoA / Disconnect | غير منفذ | allowlist وصلاحية عالية وidempotency وACK/NAK واختبار NAS |
| Accounting | جدول جلسات جاهز للقراءة | endpoint ingest موثق، upsert لـStart/Interim/Stop وحماية replay |

## قرار التشغيل

تظل واجهة الإدارة وعمليات CRUD مناسبة للاستضافة المدارة. أما مزامنة الراوترات واستهلاك RADIUS والتنبيهات الدورية فتحتاج عاملًا يعمل باستمرار أو مهام مجدولة مدروسة، مع حدود للشبكة والمهلات وإعادة المحاولة. لا يُضاف `setInterval` أو cron داخل عملية HTTP لهذا الغرض؛ إذ يجب أن يكون التنفيذ مستقلًا ومرصودًا وقابلًا للإيقاف.

يقبل دليل الراوترات بحثًا ومرشح حالة وترقيمًا داخل المؤسسة، لكن قيم الحالة هي آخر حالة محفوظة في القاعدة وليست دليلاً على اتصال حي. لا يغيّر هذا العرض حالة الراوتر ولا ينفذ اختبار اتصال من المتصفح أو من طلب الويب.

تقبل قوائم المواقع وملفات السرعة بحثًا وترقيمًا داخل المؤسسة. إنشاؤها وتصفحها لا يطبقان إعدادًا على MikroTik أو RADIUS؛ أي إسقاط لسياسة السرعة يحتاج عامل شبكة منفصلًا ومعاملات قابلة للمراجعة وقراءة نتيجة التنفيذ.

تُحدَّث حالة العميل داخل المؤسسة فقط عبر انتقالات خادمية مدققة، ولا تُعاد تنشيط حالة الأرشفة النهائية. هذا التغيير إداري داخل Netora ولا يعطّل بيانات اعتماد RADIUS أو يقطع جلسة قائمة تلقائيًا؛ تتطلب تلك الآثار مهمة عامل شبكة منفصلة ومقيدة.

تتيح لوحة دعم المنصة للمدير فقط بحث تذاكر المؤسسات الوصفية وتصفية حالتها وترقيمها. لا تتضمن هذه القراءة رسائل التذكرة أو مفاتيح التخزين أو بيانات العملاء أو إعدادات التكامل، ولا تمنح مدير المنصة إجراءً مباشرًا لتعديل تذكرة المؤسسة من هذا العرض.

تتيح قائمة مؤسسات المنصة للمدير فقط البحث بالاسم أو المعرّف والترقيم. تعرض هوية المؤسسة وحالتها وخطتها فقط ولا تستدعي عملاء المؤسسة أو جلساتها أو بيانات اعتمادها أو إعدادات تكاملها.

تتيح قائمة الاشتراكات المركزية للمدير فقط البحث بالمؤسسة أو الخطة وتصفية الحالة والترقيم. تتضمن بيانات الحوكمة التجارية اللازمة للإسناد ولا تسحب الفواتير أو العملاء أو أي تفاصيل تشغيل شبكة للمؤسسة. كما يتيح كتالوج خطط المنصة للمدير فقط البحث باسم الخطة أو رمزها وترقيم النتائج، مع إبقاء حدود الخطة ضمن العرض المركزي وعدم تمرير أي بيانات مستأجر إلى الواجهة.

تتيح قائمة باقات الخدمة البحث باسم الباقة أو ملف السرعة وتصفية حالتها وترقيمها داخل المؤسسة. تفعيل الباقة قرار إداري محلي ومراجع؛ لا يرسل هذا القرار سياسة إلى MikroTik أو RADIUS من عملية الويب.

تقبل مسودات التكامل مرجعًا بصيغة `secret://` فقط، وتتحقق من الإعدادات بصورة متداخلة لرفض أي حقل كلمة مرور أو رمز أو سر. لا تكشف قوائم التكامل المرجع المحفوظ، كما أن اختبارات العقد تغطي المرجع غير الصالح وحقول الاعتماد المتداخلة.

يسند Netora باقة خدمة مفعلة إلى عميل داخل المؤسسة فقط. تحفظ العملية تاريخ الإسناد، وتنهي الإسناد النشط السابق، وتمنع تعدد الإسنادات النشطة باستخدام مفتاح فريد قابل للتصفير. لا يعني الإسناد إنشاء بيانات اعتماد أو تطبيق ملف سرعة أو قطع جلسة على RADIUS أو MikroTik؛ تبقى هذه الآثار لمهمة عامل شبكة مستقلة وقابلة للمراجعة.

يمكن للمالك أو المدير المخول تغيير تفعيل قاعدة التنبيه المحفوظة عبر الإجراء المؤسسي المدقق نفسه. يغير هذا العلم داخل القاعدة فقط؛ لا يوجد عامل تقييم دوري أو قناة تسليم في عملية الويب الحالية. وتبقى الكتابة المالية محصورة بالمالك والمحاسب وفق مصفوفة RBAC؛ لا يملك دورا الدعم والمشاهدة صلاحية `billing:write`، وتغطي اختبارات المصفوفة هذه الحدود صراحة.

## بوابة الجاهزية قبل الإنتاج

لا ينبغي اعتماد النظام لإدارة شبكة حية قبل اكتمال اختبار العزل بين مؤسستين على قاعدة إنتاجية، واختبارات حدود الخطط مع التزامن، واستعادة نسخ احتياطي في بيئة معزولة، واختبارات RouterOS وRADIUS السعيدة والفاشلة، ومراجعة مفاتيح الأسرار، ومراقبة الطوابير والتنبيهات. كما يجب اختيار بيئة تشغيل العامل، والعناوين الخاصة، وسياسة VPN، وRPO/RTO، وسياسة الفواتير والضرائب قبل تعريض أي API شبكي للواقع.

## المراجع

[1]: https://help.mikrotik.com/docs/spaces/ROS/pages/328097/RADIUS "MikroTik RouterOS RADIUS"
[2]: https://help.mikrotik.com/docs/spaces/ROS/pages/47579162/REST+API "MikroTik RouterOS REST API"
